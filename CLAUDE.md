# קורסילנד

פלטפורמה למכירת קורסים דיגיטליים בעברית. האתר הציבורי הוא **HTML סטטי בלבד** —
אין framework, אין build step, אין תבנית משותפת. כל דף עומד בפני עצמו.

## 📖 קרא קודם: [AVI.md](AVI.md)

`AVI.md` הוא מקור האמת של הפרויקט: מפת הקבצים, תהליכי העבודה (הוספת דף, הוספת
קורס, העלאת דף שהתקבל מבחוץ), הכלים, הדשבורד והמלכודות הידועות.

**קרא אותו לפני עבודה על האתר.** יש גם סוכן ייעודי — `avi` — שנשען עליו.

## חוקים שלא נשברים

1. **`git fetch origin` לפני כל עבודה ולפני כל push.**
   כלי הניהול החיים (`admintools/`) דוחפים commits ל-`main` בעצמם דרך
   `dashboard/api/github_write.php`. **הריפו משתנה גם כשאף אחד לא עובד מקומית.**
   יש `behind`? rebase. לעולם לא `--force`.

2. **`dashboard/config.php` — קריאה בלבד.**
   סיסמאות פרודקשן חיות. לא לערוך, ולא להחזיר למעקב git (זה היה באג היסטורי
   שכבר תוקן — ראה `AVI.md` §6).

3. **כל דף שעולה לאתר מקבל את תג האנליטיקס** מיד לפני `</head>`:
   ```html
   <!-- Analytics -->
   <script src="/assets/analytics.js" defer></script>
   ```
   הכלל הוא על פרסום, לא על מי כתב את הדף — כולל דפים שמגיעים מוכנים מבחוץ.
   דף חיצוני עלול להביא סניפט `gtag`/GTM זר — להסיר, לא להשאיר לצידנו.

4. **האנליטיקס הוא אופט-אין במכוון.** `gtag` לא נטען עד שהמבקר מאשר בבאנר,
   ולכן כפתור *Test installation* של Google ידווח לנצח שהתג לא זוהה. **זו לא
   תקלה — אל תתקן.** אימות נכון: דוח Realtime ב-GA4.

5. **מדיניות הפרטיות והקוד משתנים יחד.** שינוי במה שנאסף מחייב שינוי במסמך.

6. **דף נחיתה חדש נכנס לתת-תיקייה: `pages/<לקוח>/index.html`.**
   `.gitignore` מסנן `pages/*.html`, כך שדף בשורש `pages/` **לא ייכנס ל-git
   בשקט מוחלט** — commit ו-push יצליחו והדף פשוט לא יהיה בשרת. בדיקה:
   ```bash
   git check-ignore -v pages/<שם>.html   # ריק = בסדר
   ```
   שתי הסתייגויות: הבדיקה אמינה רק על קובץ שטרם נכנס ל-index (`.gitignore`
   לא חל על קבצים tracked), ו-`pages/thankyou.html` הוא חריג מכוון עם שורת
   נגציה משלו ב-`.gitignore`. פרטים ב-`AVI.md` §8.

7. **אמת מול השרת החי.** commit אינו הוכחה:
   ```bash
   curl -s -o /dev/null -w "%{http_code}\n" https://coursyland.com/<נתיב>
   ```

## עבודה שוטפת

- ניסים עובד ישירות על `main`. אין ענפי feature בפרויקט הזה.
- `git push origin main` → האתר החי מתעדכן. אין CI ואין שלב בנייה.
- עריכה נקודתית. הדפים ארוכים ועם CSS פנימי — אל תשכתב קובץ שלם בשביל שינוי קטן.
- לבדיקה חזותית: שרת מקומי (`python3 -m http.server`). `file://` לא נפתח כאן
  בגלל נתיב בעברית.
- לביקורת רוחבית על קבצים — סקריפט שעובר קובץ-קובץ. `grep -r` כבר החמיץ קבצים
  בריפו הזה.
- הודעות commit באנגלית, גוף שמסביר *למה*, עם
  `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.

## תיעוד

גילית תהליך חדש או מלכודת חדשה? עדכן את `AVI.md` באותו commit.
