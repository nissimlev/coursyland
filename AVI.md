# אבי — מפקח העל של קורסילנד

> המסמך הזה הוא מקור האמת על האתר: איך הוא בנוי, מי כותב אליו, ואיך עושים בו
> דברים. כל שינוי מהותי במבנה או בתהליכי העבודה — מתעדים כאן באותו commit.
>
> עודכן: 29 באוגוסט 2026

---

## 1. מה זה קורסילנד

פלטפורמה למכירת קורסים דיגיטליים בעברית. שלושה חלקים נפרדים לגמרי:

| חלק | כתובת | טכנולוגיה |
|---|---|---|
| **האתר הציבורי** | `coursyland.com` | HTML סטטי, ללא build |
| **הדשבורד** | `admin.coursyland.com` | PHP 8 + MySQL |
| **כלי הניהול** | `coursyland.com/admintools/` | HTML + fetch לדשבורד |

**עיקרון מרכזי:** האתר הציבורי הוא HTML סטטי בלבד. אין framework, אין build
step, אין תבנית משותפת. כל דף עומד בפני עצמו עם `<style>` פנימי. זה מכוון —
זה מה שמאפשר לדף נחיתה לעבור ללקוח כקובץ אחד.

---

## 2. מפת הקבצים

```
/
├── index.html              קטלוג הקורסים — הדף הראשי
├── assets/analytics.js     GA4 + באנר הסכמה (משותף לכל הדפים)
├── pages/                  דפי נחיתה
│   ├── online-squad.html       תוכנית הליווי של ניסים
│   ├── asaf.html, reboot.html  דפים ישירות בתיקייה
│   └── <לקוח>/…                דפי לקוחות בתת-תיקייה
├── מסמכים/files/           4 דפי חובה משפטיים
│   └── privacy · terms · refunds · accessibility
├── images/courses/         תמונות הקורסים
├── admintools/             כלי ניהול (ראה §5)
├── dashboard/              דשבורד PHP (ראה §6)
├── sales-call-coach/       אפליקציית Python נפרדת — לא חלק מהאתר
└── jobs/ process.php status.php proxy.php    מנגנון עבודות אסינכרוני
```

`hamakpetza-online-v7.html` בשורש — דף נחיתה ישן שלא מקושר משום מקום. חי, אך
יתום. אל תמחק בלי לשאול.

---

## 3. ⚠️ שני גורמים כותבים לריפו — קרא לפני כל עבודה

זו העובדה הכי חשובה במסמך הזה.

| מחבר ב-git | מי זה | מה הוא כותב |
|---|---|---|
| `Nissim Levi` | עבודה מקומית (ניסים + קלוד) | קוד, דפים, עיצוב |
| `nissimlev` | **הכלים החיים באתר** | קורסים ותמונות |

`admintools/` כותב ישירות ל-GitHub דרך `dashboard/api/github_write.php`
(repo `nissimlev/coursyland`, branch `main`). כלומר: **הריפו משתנה גם כשאף אחד
לא עובד מקומית.** כשניסים מוסיף קורס מהאתר, נוצר commit חדש ב-main.

**לכן, לפני כל עבודה ולפני כל push:**

```bash
git fetch origin && git status -sb
```

אם יש `behind` — עשה `git rebase origin/main` לפני שאתה דוחף. אל תדחוף בכוח
לעולם; הצד השני הוא נתונים אמיתיים של ניסים.

---

## 4. דיפלוי

`git push origin main` → האתר החי מתעדכן. אין CI, אין workflow, אין שלב בנייה.

**אחרי push, אמת מול השרת האמיתי** — לא מול הקובץ המקומי:

```bash
curl -s -o /dev/null -w "%{http_code}\n" https://coursyland.com/<הנתיב>
```

ניסים עובד ישירות על `main`. אין ענפי feature בפרויקט הזה.

---

## 5. כלי הניהול — `admintools/`

ארבעה כלים, כולם דפי HTML שמדברים עם הדשבורד:

| כלי | תפקיד |
|---|---|
| `add-course/` | מוסיף קורס לקטלוג + מעלה תמונה |
| `edit-course/` | עורך קורס קיים |
| `delete-course/` | מוחק קורס |
| `sales-page-builder/` | בונה דפי מכירה. מכיל `SKILL.md` — סקיל של 15 חלקים |

כולם עוברים דרך `dashboard/api/github_write.php`, שדורש session פעיל של
הדשבורד + csrf_token. הטוקן של GitHub יושב מחוץ ל-public_html ולא מגיע
לדפדפן.

---

## 6. הדשבורד — `dashboard/`

PHP + MySQL ב-`admin.coursyland.com`. מנהל לקוחות, קורסים, מכירות ודוחות.
משולב עם **iCount** (חשבוניות) ו-**Gmail SMTP** (מיילים), ומייצר PDF עם mpdf.

### 🔴 חוק קשיח: לא נוגעים ב-`dashboard/config.php`

הקובץ מכיל סיסמאות פרודקשן חיות. **קריאה בלבד, ולעולם לא עריכה.** אם צריך
להבין את המבנה — קרא את `config.example.php`.

### מה שכבר תוקן ואין לשחזר

בעבר `config.php` היה במעקב git, וכל דיפלוי דרס את הקובץ שבשרת בגרסה ישנה עם
סיסמת DB שגויה → הדשבורד החזיר 500. **זה תוקן:**

- הקובץ ירד מ-git ונמצא ב-`dashboard/.gitignore`
- `includes/config_load.php` מתפקד גם בלי הקובץ ומשלים ברירות מחדל ריקות
- הסודות האמיתיים יושבים מחוץ ל-public_html (`db_login.txt`, `admin_login.txt`)

**אל תחזיר את `config.php` למעקב git.** בדיקת בריאות: `/dashboard/` אמור
להחזיר 302 (הפניה ללוגין). 500 = תקלה אמיתית, לא הבאג ההיסטורי.

---

## 7. אנליטיקס — הכלל שחל על כל דף

GA4 `G-68WQD2QLSZ`, נטען מקובץ משותף אחד.

**כל דף שעולה לאתר חייב את השורה הזו, מיד לפני `</head>`:**

```html
<!-- Analytics -->
<script src="/assets/analytics.js" defer></script>
```

הכלל הוא על **פרסום, לא על מי כתב את הדף**: דפים שקלוד בנה, דפים שניסים מביא
מוכנים, ייצוא מכלי אחר, העתקה מאתר ישן. אם המשימה היא "תעלה את הדף הזה" —
הוספת התג היא חלק מהמשימה.

- הנתיב מוחלט, ולכן זהה מכל עומק תיקייה
- **אין** להדביק סניפט `gtag` בתוך הדף עצמו
- דף שמגיע מבחוץ עלול להביא סניפט משלו או מזהה זר — **להסיר**, לא להשאיר לצד שלנו
- `landing-page-creator` ו-`sales-page-builder` **לא** מוסיפים את התג לבד
- פטורים: `dashboard/` ו-`admintools/` — הקוד ממילא מסרב לרוץ שם

### המודל: אופט-אין

`gtag` לא נטען עד שהמבקר לוחץ "אני מאשר" בבאנר. **תוצאה שנראית כמו באג ואיננה:**
כפתור *Test installation* של Google ידווח לנצח *"tag wasn't detected"* — הבוט
שלו לא לוחץ על באנרים.

**אל תתקן את זה** בטעינת gtag ללא תנאי. זה ישבור את מודל ההסכמה ויהפוך את
סעיף 5 במדיניות הפרטיות לשקר. אימות נכון: דוח **Realtime** ב-GA4.

מדיניות הפרטיות והקוד חייבים להשתנות יחד. תמיד.

---

## 8. תהליכי עבודה

### הוספת דף נחיתה חדש

1. `git fetch origin` ווידוא שאתה מסונכרן

2. **מיקום — כאן נמצאת המלכודת הגדולה ביותר בפרויקט.**

   `.gitignore` שורה 3 מכילה `pages/*.html`. כלומר **דף חדש ישירות ב-`pages/`
   לא ייכנס ל-git, בשקט מוחלט**: `git add` ידלג עליו, ה-commit יעבור,
   ה-push יצליח — והדף לא יהיה קיים בשרת.

   ```bash
   git check-ignore -v pages/<שם>.html   # ריק = בסדר. פלט = הקובץ מסונן
   ```

   - ✅ **`pages/<לקוח>/index.html`** — ברירת המחדל. תת-תיקיות אינן מסוננות,
     וזה גם נותן URL נקי: `coursyland.com/pages/<לקוח>/`
   - `pages/<לקוח>/<שם>.html` — תקין גם כן, בשימוש ב-`sharona/lehithaber.html`
     וב-`dikla-teler/present-of-gaps.html`
   - ❌ `pages/<שם>.html` — **להימנע.** `asaf`, `reboot` ו-`online-squad`
     נמצאים שם רק מלפני הכלל, וזה מה שגורם לזה להיראות תקין

3. `<html lang="he" dir="rtl">`, `<style>` פנימי, mobile-first

4. **תג האנליטיקס לפני `</head>`** (§7)

5. `<footer>` — קישור "הגדרות פרטיות" נשתל אליו אוטומטית. שרשרת הנסיגה
   ב-`analytics.js` היא `footer` → `.wrap` → `body`, כלומר בלי footer הקישור
   ינחת בסוף ה-body. תן לדף footer.
6. אם הדף צריך להופיע בקטלוג — §"הוספת קורס"
7. commit → push → אימות ב-curl מול השרת החי

### העלאת דף שהתקבל מבחוץ

1. **קרא את הדף במלואו לפני שאתה מעלה** — גם אם נאמר לך שהוא מוכן
2. בדוק `<head>`: הסר `gtag`/GTM זרים, הוסף את התג שלנו
3. בדוק קישורים חיצוניים ותשלומים שמצביעים לגורם אחר
4. משם — כמו דף רגיל

### הוספת קורס לקטלוג

הקטלוג נבנה ממערך JS ב-`index.html` (`const courses = [...]`, סביב שורה 628).
קורסים חדשים נכנסים **בראש המערך**.

```js
{
  id: 17,
  title: "שם הקורס",
  desc: "תיאור. \n שורה שנייה",
  category: "זוגיות",              // ראה קטגוריות קיימות למטה
  price: "₪345",
  oldPrice: "₪1477",               // אופציונלי
  duration: "7 שיעורים",
  instructor: "שם המנחה",
  img: "https://raw.githubusercontent.com/nissimlev/coursyland/main/images/courses/<קובץ>",
  sample: false,                   // true = קורס דוגמה, מוצג בסוף
  link: "https://coursyland.com/pages/<...>.html"
}
```

קטגוריות בשימוש: `זוגיות` · `מיינדסט` · `בריאות והורות` · `עסקים דיגיטליים` ·
`תקשור`. אל תמציא קטגוריה חדשה בלי לשאול.

**עדיף שניסים יוסיף קורס דרך `admintools/add-course/`** — זה מטפל גם בתמונה.
עשה זאת ידנית רק כשהוא מבקש במפורש.

### שינוי בדף קיים

הדפים ארוכים ועם `<style>` פנימי. ערוך נקודתית, אל תשכתב קובץ שלם.
אמת בדפדפן לפני commit — הרבה דפים משתמשים ב-`.fade` שמופעל ב-IntersectionObserver,
אז צריך להוסיף להם `.in` ידנית כדי לראות תוכן בצילום מסך.

---

## 9. קונבנציות

**עברית ו-RTL** — `dir="rtl"`, גופן `Heebo` (ובחלק מהדפים `Secular One`
לכותרות), מספרים ותאריכים בתוך `<bdi dir="ltr">`.

**הודעות commit** — באנגלית, שורת נושא בזמן הווה ציווי, גוף מסביר *למה*.
תמיד עם `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.

**אימות** — הרץ שרת מקומי (`python3 -m http.server`) ובדוק בדפדפן. `grep -r`
כבר החמיץ קבצים בריפו הזה, אז לביקורת רוחבית עדיף סקריפט שעובר קובץ-קובץ.

---

## 10. מלכודות ידועות

| מלכודת | מה קורה | מה לעשות |
|---|---|---|
| **`pages/*.html` ב-`.gitignore`** | **דף חדש בשורש `pages/` לא נדחף — בלי שום שגיאה** | **תת-תיקייה: `pages/<לקוח>/index.html`** |
| הריפו זז מעצמו | הכלים החיים דוחפים ל-main | `git fetch` לפני כל עבודה |
| `grep -r` לא אמין כאן | החמיץ 3 קבצים ב-`pages/` | ביקורת קובץ-קובץ |
| בדיקת התג של Google נכשלת | מודל אופט-אין | תקין. אמת ב-Realtime |
| `file://` לא נפתח בדפדפן | נתיב עם עברית | שרת מקומי במקום |
| `config.php` | סודות פרודקשן | קריאה בלבד. לעולם לא ב-git |
| מדיניות פרטיות וקוד | נוטים להיפרד | משנים אותם יחד |

---

## 11. איך אבי עובד

1. **קודם קורא, אחר כך משנה.** תמיד `git fetch`, תמיד בדיקת המצב בפועל.
2. **מתקן את השורש, לא את הסימפטום.** אם משהו נראה כמו באג, בדוק אם זו
   התנהגות מכוונת (בדיקת התג של Google היא הדוגמה החיה).
3. **אומר כשמשהו לא נכון.** אם בקשה מתנגשת עם מסמך משפטי, עם סודות, או עם
   נתונים חיים — לומר לפני, לא אחרי.
4. **מאמת מול השרת החי.** "עשיתי commit" זה לא "זה עובד".
5. **מתעד כאן.** תהליך חדש או מלכודת חדשה — נכנסים למסמך הזה.
